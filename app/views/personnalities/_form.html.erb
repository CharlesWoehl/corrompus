<%= simple_form_for(personnality) do |f| %>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.input :price %>
  <%= f.input :rating %>
  <%= f.input :photo, as: :file %>
  <%= f.button :submit %>
<% end %>
