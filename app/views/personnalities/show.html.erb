<div class="sous-container">
  <div class="personnality-details">
    <div class="personnality-header">
      <div class="images">
        <%= cl_image_tag @personnality.photo.key, crop: :fill %>
        <a class="btn btn-ghost" href="#"><i class="fa-solid fa-fire"></i> <%= @personnality.punchline %></a>
      </div>
      <div class="skills">
        <h1><%= @personnality.name  %></h1>

      </div>
    </div>

    <div class="personnality-content">
     <div class="d-flex justify-content-around p-3 mb-4">



<% @joinskills.where(personnality_id: @personnality.id).each do |join| %>
 <div class="d-flex flex-column text-center personnality-infos">
        <div class="icone-list-pers rose"> <i class="<%= join.skill.icone %>"></i></div>
        <div class="name-list-pers"><%= join.skill.name %></div>
      </div>
<% end %>
<% @joinskills.where.not(personnality_id: @personnality.id).each do |join| %>
 <div class="d-flex flex-column text-center personnality-infos">
        <div class="icone-list-pers"> <i class="<%= join.skill.icone %>"></i></div>
        <div class="name-list-pers"><%= join.skill.name %></div>
      </div>
<% end %>

</div>

      <h2>Mon experience :</h2>
      <p><%= simple_format(@personnality.description) %></p>


    </div>
  </div>

  <div class="purchase-sidebar sidestyle">
    <div class="title">
      <span><h3><strong><%= @personnality.price %>€</strong></h3></span>
      <span><p class="m-2">par jour</p></span>
      <span class="rating"><p class="card-trip-pricing d-flex"><i class="fa-solid fa-star rose px-1"></i> <%= @personnality.rating %></p></span>
    </div>
    <%= render "booking-form", personnality: @personnality %>
  </div>
</div>
